# sql_column_extractor
This hosts the python library code for extracting column names from a SQL query. The SQL query can be a simple select, select with inline queries, queries with "with" clause and many more.
